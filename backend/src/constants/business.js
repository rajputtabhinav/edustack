import { env } from "../config/env.js";

export const NOTE_PRICE = env.notePrice;
export const REFERRAL_REWARD = env.referralReward;
export const REFERRAL_JOIN_BONUS = env.referralJoinBonus;
export const MIN_WITHDRAWAL = env.minWithdrawal;
